# Przygotowuje dane do uczenia maszynowego ( w sensie imputacja braków danych, normalizacja cech oraz kodowanie zmiennych)

import numpy as np
import pandas as pd

from src.config import TARGET_COLUMN, NUMERIC_FEATURES, CATEGORICAL_FEATURES

